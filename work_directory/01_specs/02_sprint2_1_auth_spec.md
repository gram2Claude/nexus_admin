# Спека спринта 2.1 — БД кабинета и аутентификация (NEXADM-5…7)

Статус: **СОГЛАСОВАНО** (2026-06-11: seed — вариант B «временный пароль в env»; Owner —
gram2.0ai@gmail.com; сессия 30 дней; матрица прав ок, Employee затраты НЕ видит)
Задачи: t05 схема БД + seed Owner · t06 Auth.js · t07 RBAC.
План: 16.06–18.06 (идём с опережением — спринт 1.1 закрыт 10.06).

---

## 1. t05 — Схема БД и первый Owner

Таблицы в отдельной схеме `nexus_admin` той же Supabase-БД: `users` (email, имя, роль,
hash пароля, статус), `invites` (токен, email, роль, срок, использован), миграции forward-only.

**Seed первого Owner — 3 варианта:**
- **A. Инвайт-ссылка (РЕКОМЕНДАЦИЯ):** seed создаёт Owner-запись без пароля + одноразовую ссылку
  установки пароля (печатается в консоль при деплое). Пароль нигде не хранится и не передаётся.
- **B. Временный пароль в env:** быстрее, но пароль светится в конфиге до первой смены.
- **C. Captive first-run:** первый зашедший становится Owner. Просто, но опасно на публичном IP.
ОТВЕТ (вариант seed):Временный пароль

**Email первого Owner** — предлагаю `corpai86@gmail.com` (твой основной).
ОТВЕТ (email ок / другой?): gram2.0ai@gmail.com

---

## 2. t06 — Аутентификация (решено планом, вопрос один)

Auth.js (credentials) + bcrypt + JWT-сессии; всё кроме `/login` и страницы установки пароля
закрыто middleware. Fallback при граблях Next 16 — Supabase Auth (зафиксирован в рисках).

**Срок жизни сессии** — предлагаю 30 дней со скользящим продлением (вход раз в месяц с устройства).
ОТВЕТ (30 дней ок?): ок

---

## 3. t07 — Матрица прав роль×действие (НА СОГЛАСОВАНИЕ)

Это артефакт done-критерия t07 (решение ревью плана №11). Server-side проверки строятся по ней.

| Действие | Owner | Admin | Employee | Client |
|---|:-:|:-:|:-:|:-:|
| Видеть все проекты | ✔ | ✔ | только свои | позже |
| Видеть затраты (токены, ≈$, часы) | ✔ | ✔ | ✖ | ✖ |
| Drill-down до моделей AI | ✔ | ✔ | по своим проектам | ✖ |
| Пригласить пользователя / удалить / сменить роль | ✔ | ✔ (кроме Owner) | ✖ | ✖ |
| Назначить роль Admin | ✔ | ✖ | ✖ | ✖ |
| Удалить / понизить Owner | никто | ✖ | ✖ | ✖ |
| Редактировать описания проектов | ✔ | ✔ | ✖ | ✖ |
| Назначать участников проекта (membership) | ✔ | ✔ | ✖ | ✖ |
| Привязка чата к проекту (раздел «Чаты») | ✔ | ✔ | ✖ | ✖ |
| Служебная страница /styleguide | ✔ | ✔ | ✖ | ✖ |

Права Client — заглушка «всё ✖» до отдельного решения (роль про запас, как договорились).

> **Ревизия 2026-06-13 (E11, TIME-74):** добавлено действие «Привязка чата к проекту» (раздел
> «Чаты», запись `tg_assistant.tg_chat_bindings`) — Owner/Admin. Чтение раздела «Чаты»
> (дайджесты/темы/журнал) подчиняется правилу «Видеть все проекты» (Employee — только свои,
> по membership; реализовано в `src/server/chats.ts`, TIME-73). Кодом: `can.bindChats`.

**Вопрос:** Employee видит затраты (токены/стоимость/часы) по СВОИМ проектам — да или только
прогресс/задачи без денег? (влияет на t17/t26/t28)
ОТВЕТ (матрица ок + Employee и затраты): Employee не видит затраты (токены/стоимость/часы).только
прогресс/задачи без денег 

---

## 4. ASCII-схемы служебных страниц auth (конвенция «схема до визуализации»)

Страница /login (реализуется в t06; формально схемы числятся за t08 — выношу сюда заранее):

```
┌──────────────────────────────────────────────┐
│                                              │
│              [▣] Nexus Admin                 │
│            кабинет управленца                │
│                                              │
│   ┌──────────────────────────────────────┐   │
│   │  Вход                                │   │
│   │  ┌─────────────────────────────────┐ │   │
│   │  │ Email                           │ │   │
│   │  └─────────────────────────────────┘ │   │
│   │  ┌─────────────────────────────────┐ │   │
│   │  │ Пароль                       👁 │ │   │
│   │  └─────────────────────────────────┘ │   │
│   │  (ошибка: «Неверный email/пароль»)   │   │
│   │  [        Войти (тёмная)         ]   │   │
│   └──────────────────────────────────────┘   │
│   Вход только по приглашению Owner/Admin     │
└──────────────────────────────────────────────┘
```

Страница установки пароля по инвайту (реализуется в t10, спринт 2.2) — та же рамка, поля:
«Пароль» + «Повторите пароль», кнопка «Сохранить и войти», при невалидном/просроченном
токене — сообщение «Ссылка недействительна, запросите новое приглашение».
ОТВЕТ (схемы ок?): схема установки пароля ок (2026-06-11; /login реализована и принята вживую)

## 5. ИТОГОВЫЙ SCOPE

1. **t05 (NEXADM-5):** схема `nexus_admin` в Supabase Postgres: `users` (email unique, имя, роль
   owner/admin/employee/client, hash пароля bcrypt, статус invited/active/disabled), `invites`
   (token_hash, email, роль, срок, использован), `schema_migrations`; forward-only мигратор
   (`npm run db:migrate`); **seed Owner: вариант B** — email `gram2.0ai@gmail.com`, временный
   пароль из env `OWNER_TEMP_PASSWORD` (только в `.env.local`/env сервера, НЕ в репо).
2. **t06 (NEXADM-6):** Auth.js v5 (credentials) + bcryptjs; JWT-сессия **30 дней** со скользящим
   продлением; middleware: всё кроме `/login`, `/set-password`, `/api/auth` — закрыто;
   страница `/login` по схеме п.4; выход — в меню пользователя в header (вместо заглушки —
   реальные имя/роль из сессии).
3. **t07 (NEXADM-7):** матрица прав п.3 кодом (`src/lib/rbac.ts`) + server-side guard'ы:
   `/users` и `/styleguide` — только Owner/Admin; пункт «Пользователи» в sidebar скрыт для
   Employee/Client; **Employee не видит затраты** (токены/стоимость/часы) — закреплено в rbac
   как `seeCosts` (понадобится в t17/t26/t28).
