# QA-отчёт спринта 3.2 — сверка факта с timechecker (NEXADM-19)

Дата: 2026-06-11. Среда: реальная Supabase БД (реплика timechecker), view-слой миграции 004.

| # | Проверка | Результат |
|---|---|---|
| 1 | Маппинг план↔факт: `tasks.readable_id` ↔ `public.task.identifier` | ✅ все NEXADM-задачи находят пару; ключи canon_task_id совпадают |
| 2 | `v_task_fact`: часы/токены/стоимость по задачам NEXADM | ✅ (например NEXADM-7: 0.6 ч, 978 783 ток., $369.35) |
| 3 | Исполнитель задачи | ✅ «Oleg» (COALESCE display_name → windows_username — display_name в timechecker пуст) |
| 4 | **Сверка сумм** «задача × модель» против `daily_agent_usage` — ВСЕ задачи с данными (после правок ревью) | ✅ **35/35 задач, max 0.0000%**; полнота: Σ view $10 853.12 vs Σ dau $10 853.10 (округление); минуты: 20/20 задач сходятся с v_task_fact точно |
| 5 | Roll-up уровней (задачи → спринты → эпохи) через levelFactRollup | ✅ суммы детей = сумме уровня (один источник v_task_fact) |
| 6 | Бакет «нераспределённое» | ✅ выделен (история без задач: opus-4-8 $7 471, fable-5 $1 156, gpt-5.5 $15) — уровень портфеля (см. отклонение в спеке п.1) |
| 7 | Производительность: полный проход `v_task_model_fact`, 3 прогона | ✅ 34/34/35 мс (p95 < 500 мс — запас 15×) |
| 8 | Границы роли приложения: SELECT public.task / INSERT public.task / UPDATE sync_meta под `nexus_admin_app` | ✅ 3/3 отклонено; view-доступ работает; кабинет (вход, /projects, /users) работает под ограниченной ролью |

## Замечания

- Модельная разбивка — дневное приближение (task_id у сессий NULL — ограничение данных
  timechecker, зафиксировано в спеке 3.2 п.1); точные числа задачи — из daily_agent_usage.
- Сегодняшние задачи показывают нули до вечернего пересчёта метрик timechecker (23:50) — это
  лаг конвейера факта, не дефект view.

Дефектов не найдено.
