# Двойное ревью кода эпохи 7 (NEXADM-34,35 — профиль) — итоги триажа

Дата: 2026-06-11. Ревьюеры: Claude-субагент + codex (high). Объект: `76ebef4`, `daa2235`.
Применено в `<следующий коммит>`.

## Применено (10 правок)

| # | Находка | Кто | Правка |
|---|---|---|---|
| 1 | **Смена пароля не гасила другие JWT-сессии (30 дней)** — украденный/забытый cookie переживал смену | codex P1 / субагент MED | миграция 005 `pw_changed_at`; токен хранит метку на момент входа; 10-мин ревалидация сверяет с БД → чужая сессия гаснет; инициатор живёт (unstable_update обновляет его токен, trigger='update'). Смоук с ускоренной ревалидацией: сессия B умерла, A жива |
| 2 | Лимит bcrypt — 72 БАЙТА utf-8, не символа (кириллица >36 символов молча усекалась) | codex P2 / субагент NIT | Buffer.byteLength в changePassword и set-password |
| 3 | `current` не ограничен перед bcrypt.compare (мегабайтные строки) | субагент LOW | guard 72 байта + registerFail |
| 4 | Новый SELECT в layout без catch — сбой ронял весь кабинет (фолбэк из JWT уже был написан) | субагент LOW | .catch → rows:[] |
| 5 | Политика длины расходилась: инвайт min 8, профиль min 12 | субагент LOW | set-password выровнен до 12 (action + форма) |
| 6 | Хинт «отображается в колонке Исполнитель» неверен (исполнитель — из timechecker) | субагент LOW | текст: «в шапке и списках участников проектов» |
| 7 | Установка пароля по инвайту не ставила pw_changed_at | вытекает из №1 | consumeInvite ставит now() |
| 8 | updateName без проверки status='active' (окно 10 мин у disabled) | субагент NIT | AND status='active' |
| 9 | Нет скрытого username для менеджеров паролей; нет autoComplete=name | субагент NIT | добавлены |
| 10 | «Имя сохранено» не сбрасывалось при правке поля | субагент NIT | onChange сбрасывает |

Также: REVALIDATE_MS получил env-override (AUTH_REVALIDATE_MS) — только для смоук-тестов
инвалидации; в проде не задан.

## Чисто (проверено обоими)

Порядок проверок changePassword (isBlocked до compare, fail только на неверный пароль,
dummy-compare не нужен — свой хэш); утечек пароля в логи/ошибки нет; session.user.id
доверяем (подписанный JWE); XSS через имя нет (JSX-интерполяция, без dangerouslySetInnerHTML);
+1 PK-запрос в layout параллелен freshness и не летит на soft-навигации;
revalidatePath('/','layout') достаточно; двойного сабмита нет; XFF не спуфится
(только через Caddy); автокомплиты паролей корректны.

## Верификация после правок

build ✓ lint ✓ миграция 005 применена ✓ · смоук инвалидации: B гаснет, A живёт,
вход новым паролем ОК ✓.
