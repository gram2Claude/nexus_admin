import { MessagesSquare } from "lucide-react";

import { SectionStub } from "@/components/layout/section-stub";

export default function ChatsPage() {
  return (
    <SectionStub
      icon={MessagesSquare}
      title="Чаты"
      description="Раздел запланирован — наполнение добавит управленец по мере продвижения."
    />
  );
}
