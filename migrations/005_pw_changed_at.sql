-- 005: метка смены пароля — инвалидация чужих JWT-сессий после смены (ревью эпохи 7).
-- Токен хранит pwt на момент входа; 10-минутная ревалидация сверяет с БД:
-- в БД новее → сессия гаснет (украденный/забытый cookie не переживает смену пароля).
ALTER TABLE nexus_admin.users
  ADD COLUMN IF NOT EXISTS pw_changed_at timestamptz;
