-- 006: второй Owner — решение управленца 2026-06-11 (ревизия ограничения из 002).
-- Снимаем единственность Owner (ux_users_single_owner). Матрица прав (rbac.ts)
-- не меняется: Owner неприкосновенен для всех (canModifyUser), поэтому
-- совладельцы не могут удалить/понизить друг друга — только вручную через SQL.
-- Инвайт Owner'а в UI по-прежнему недоступен (INVITABLE_ROLES) — создаётся
-- адресным скриптом координатора; в строке invites при этом role='admin'
-- (CHECK таблицы), роль на users-строке ставится сразу, активация её не трогает.
DROP INDEX IF EXISTS nexus_admin.ux_users_single_owner;
